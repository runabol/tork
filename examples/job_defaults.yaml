name: job defaults example

defaults:
  limits:
    cpu: .5

tasks:

  - name: sleep a little
    image: alpine:3.18.3
    run: sleep 0.1

  - name: sleep too much
    image: alpine:3.18.3
    run: sleep 2